services:
  - type: web
    name: africa-education-api
    runtime: python
    rootDir: summative/API
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn prediction:app --host 0.0.0.0 --port $PORT
    plan: free
